name: Section 8 Format

on:
  push:
    branches: [refinement/youtube-playlists-section-8]

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refinement/youtube-playlists-section-8
      - run: corepack enable
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write src/app/dashboard/youtube/page.tsx
      - name: Commit formatting when needed
        run: |
          if ! git diff --quiet; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add src/app/dashboard/youtube/page.tsx
            git commit -m "style: format Section 8 file"
            git push
          fi
# Trigger formatter after workflow creation.
